import { ToastOptions } from 'ng2-toastr/ng2-toastr';

export let options: ToastOptions = new ToastOptions({
  // animate: 'flyRight',
  positionClass: 'toast-bottom-right',
  toastLife: 50000,
  // iconClass: 'toast-pink'
});
